package org.example.javaspringboot.model;

/**
 * Enum representing different types of devices.
 * It categorizes devices into specific types like HEAT_DEVICE and LIGHT_DEVICE.
 */
public enum DeviceType {
    HEAT_DEVICE,
    LIGHT_DEVICE
}
